import { StyleSheet } from "react-native"
import { colors } from "../../styles/colors"

export const styles = StyleSheet.create({
    container: {
        height: 57,
        width: '100%',
        backgroundColor: colors.colorsPrimaryTheme.primary,
        justifyContent: 'center',
        alignItems: 'center'
    }
}) 
